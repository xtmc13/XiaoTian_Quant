package cra

// CRAContractStrategy is the runtime type for CRA contract strategies.
// It shares the same implementation as BaseCRAStrategy with isContract=true.
type CRAContractStrategy = BaseCRAStrategy
